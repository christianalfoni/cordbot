# Web Service URL (for OAuth redirects)
WEB_SERVICE_URL=https://cordbot.io

# NOTE: Google OAuth credentials (GOOGLE_CLIENT_ID and GOOGLE_CLIENT_SECRET)
# should be stored as Firebase secrets, not environment variables.
# See documentation for setup instructions:
#
# firebase functions:secrets:set GOOGLE_CLIENT_ID
# firebase functions:secrets:set GOOGLE_CLIENT_SECRET
