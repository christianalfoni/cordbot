{
  "buildCommand": "pnpm install && pnpm --filter @cordbot/web-service build",
  "outputDirectory": "packages/web-service/dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
