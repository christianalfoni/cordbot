{
  "name": "claudebot-monorepo",
  "version": "1.0.0",
  "private": true,
  "description": "Discord bot powered by Claude Code SDK - monorepo",
  "scripts": {
    "dev:bot": "pnpm --filter bot dev",
    "build": "pnpm -r build",
    "test": "pnpm -r test"
  },
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=8.0.0"
  }
}
